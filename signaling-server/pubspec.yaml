name: signaling_server
description: A WebSocket signaling server for peer-to-peer communication
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^2.4.0

