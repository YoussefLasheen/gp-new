name: signaling_server
description: WebRTC signaling server
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.3
  http: ^1.1.0
  dotenv: ^3.0.0

dev_dependencies:
  lints: ^3.0.0

